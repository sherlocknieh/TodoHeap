# TodoHeap 前端

- 安装 Node.js
    ```powershell
    winget install OpenJS.NodeJS
    # 配置 npm 源为国内镜像
    npm config set registry https://registry.npmmirror.com
    ```
- 安装项目依赖
    ```bash
    npm install
    ```
- 运行
    ```bash
    npm run dev
    ```
